# Frontend .env.example
# This file contains frontend-specific placeholders. Shared/secret variables
# (upload API keys, allowed origins, firebase admin service account, etc.)
# are centralized in the repository root `.env.example`.
#
# Copy `.env.example` (root) -> `.env.local` and then edit frontend-specific values.

# Frontend-specific configuration
# Base API URL used by the browser (no trailing slash)
NEXT_PUBLIC_API_URL=http://localhost:3000

# Public Firebase config (client-side, safe-ish to include placeholders)
NEXT_PUBLIC_FIREBASE_API_KEY=your-firebase-api-key
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your-project-id
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your-project.appspot.com
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=your-sender-id
NEXT_PUBLIC_FIREBASE_APP_ID=your-app-id

# MercadoPago (public key)
NEXT_PUBLIC_MERCADOPAGO_PUBLIC_KEY=APP_USR-xxxxx

# Environment (development|production)
NODE_ENV=development
